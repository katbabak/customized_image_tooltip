export enum POSITION_X {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right'
}

export enum POSITION_Y {
  TOP = 'top',
  BOTTOM = 'bottom'
}

export enum DIALOG_STATES {
  CREATE = 'CREATE',
  DETAILS = 'DETAILS'
}
